# 既存資産移植リスト

**作成日**: 2025-10-18
**目的**: 旧プロジェクト（my-ai-diary）から新プロジェクト（my-ai-diary-v2）への移植資産管理

---

## 📦 移植対象ファイル一覧

### ✅ 最優先（そのままコピー可能）

#### UIコンポーネント

| 旧ファイルパス | 新ファイルパス | サイズ | 修正必要度 |
|--------------|--------------|-------|----------|
| `src/components/ChatView.tsx` | `src/components/chat/ChatView.tsx` | 40行 | ⭐ 低（API呼び出し変更のみ） |
| `src/components/Message.tsx` | `src/components/chat/Message.tsx` | 60行 | ⭐ なし |
| `src/components/MessageList.tsx` | `src/components/chat/MessageList.tsx` | 80行 | ⭐ なし |
| `src/components/MessageInput.tsx` | `src/components/chat/MessageInput.tsx` | 50行 | ⭐ 低 |
| `src/components/DiaryView.tsx` | `src/components/diary/DiaryView.tsx` | 200行 | ⭐⭐ 中（データ取得変更） |
| `src/components/CalendarView.tsx` | `src/components/diary/CalendarView.tsx` | 150行 | ⭐ 低 |

#### 型定義

| 旧ファイルパス | 新ファイルパス | サイズ | 修正必要度 |
|--------------|--------------|-------|----------|
| `src/types/index.ts` | `src/types/index.ts` | 40行 | ⭐ 低（型追加のみ） |

### 🔧 中優先（部分修正が必要）

| 旧ファイルパス | 新ファイルパス | サイズ | 修正内容 |
|--------------|--------------|-------|---------|
| `src/components/Header.tsx` | `src/components/layout/Header.tsx` | 100行 | ナビゲーション追加、認証状態表示 |
| `src/components/AgentSettings.tsx` | `src/components/settings/AgentSettings.tsx` | 120行 | プリセット機能、保存API変更 |
| `src/components/DiaryGenerator.tsx` | `src/components/diary/DiaryGenerator.tsx` | 80行 | サブスク連携、API変更 |

### 📚 参考用（ロジックを参考にする）

| 旧ファイルパス | 参考箇所 | 活用方法 |
|--------------|---------|---------|
| `src/services/openai.ts` | プロンプト設計、エラーハンドリング | 新API実装時に参考 |
| `src/services/database.ts` | データ構造、CRUD操作 | Supabase実装の参考 |
| `src/hooks/useDatabase.ts` | 状態管理、データフェッチ | カスタムフック設計の参考 |

### ❌ 移植不要（置き換え）

| 旧ファイルパス | 理由 | 新実装 |
|--------------|------|--------|
| `src/components/ApiKeySettings.tsx` | サーバーサイドで管理に変更 | 不要 |
| `src/components/DebugInfo.tsx` | 開発用、本番不要 | Vercel Analytics |
| `src/App.tsx` | App Routerに移行 | `app/layout.tsx` + ページ構造 |

---

## 📋 移植チェックリスト

### Phase 1: 基本コンポーネント

- [ ] `ChatView.tsx` をコピー
  - [ ] import文修正
  - [ ] API呼び出し先変更（`/api/chat` に）
  - [ ] 型定義確認

- [ ] `Message.tsx` をコピー
  - [ ] そのまま動作確認

- [ ] `MessageList.tsx` をコピー
  - [ ] そのまま動作確認

- [ ] `MessageInput.tsx` をコピー
  - [ ] onSubmit処理確認

- [ ] `DiaryView.tsx` をコピー
  - [ ] データ取得をSupabaseに変更
  - [ ] フィルタリング機能確認

- [ ] `CalendarView.tsx` をコピー
  - [ ] 日付クリック時の挙動確認

### Phase 2: レイアウト・設定

- [ ] `Header.tsx` を改良
  - [ ] ナビゲーションメニュー追加
  - [ ] ユーザーアバター表示
  - [ ] サブスク状態表示

- [ ] `AgentSettings.tsx` を改良
  - [ ] プリセット性格5種類追加
  - [ ] 保存処理をSupabaseに変更
  - [ ] バリデーション追加

- [ ] `DiaryGenerator.tsx` を改良
  - [ ] サブスク確認追加
  - [ ] 使用量チェック追加
  - [ ] ローディング状態改善

### Phase 3: ユーティリティ

- [ ] `types/index.ts` を拡張
  - [ ] Supabase型追加
  - [ ] Stripe型追加
  - [ ] 認証関連型追加

---

## 🎨 スタイルガイド（移植時の注意）

### Tailwind CSS クラス名の互換性

**旧プロジェクト（v3.4）と新プロジェクト（v4）の違い:**

大部分は互換性があるが、以下に注意：

```tsx
// v3.4（旧）
className="bg-gray-50 hover:bg-gray-100"

// v4（新）- そのまま使える
className="bg-gray-50 hover:bg-gray-100"

// 一部の新機能（v4のみ）
className="bg-gray-50/50"  // 透明度の新記法
```

### レスポンシブデザイン

既存コンポーネントはモバイルファーストで設計済み。
そのまま使用可能。

```tsx
// 既存の良い例
<div className="px-4 md:px-8 lg:px-16">
```

---

## 🔄 移植ワークフロー

### ステップ1: コピー

```bash
# 旧プロジェクトから新プロジェクトへ
cd /Users/fumiyasu/develop/my-ai-diary

# コンポーネントをコピー
cp src/components/ChatView.tsx ../my-ai-diary-v2/src/components/chat/
cp src/components/Message.tsx ../my-ai-diary-v2/src/components/chat/
cp src/components/MessageList.tsx ../my-ai-diary-v2/src/components/chat/
cp src/components/MessageInput.tsx ../my-ai-diary-v2/src/components/chat/
cp src/components/DiaryView.tsx ../my-ai-diary-v2/src/components/diary/
cp src/components/CalendarView.tsx ../my-ai-diary-v2/src/components/diary/

# 型定義をコピー
cp src/types/index.ts ../my-ai-diary-v2/src/types/
```

### ステップ2: 修正

各ファイルを開いて、以下を確認・修正：

1. **Import文**
   ```typescript
   // 旧
   import { MessageData } from '../types';

   // 新
   import { MessageData } from '@/types';
   ```

2. **API呼び出し**
   ```typescript
   // 旧
   const response = await openAIService.sendMessage(messages, systemPrompt);

   // 新
   const response = await fetch('/api/chat', {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({ messages, systemPrompt })
   });
   ```

3. **データ取得**
   ```typescript
   // 旧
   const data = await loadTodayConversation();

   // 新
   const { data } = await supabase
     .from('conversations')
     .select('*')
     .eq('date', today)
     .single();
   ```

### ステップ3: テスト

```bash
# 開発サーバーで確認
cd my-ai-diary-v2
pnpm dev

# ブラウザで各コンポーネントの動作確認
# http://localhost:3000
```

### ステップ4: リファクタリング（オプション）

動作確認後、以下を検討：
- コードの最適化
- コメントの追加
- 型の厳密化
- アクセシビリティの向上

---

## 📊 移植進捗トラッキング

### Week 1 目標

| コンポーネント | 状態 | 担当 | 完了日 |
|--------------|------|------|--------|
| ChatView.tsx | ⬜ 未着手 | - | - |
| Message.tsx | ⬜ 未着手 | - | - |
| MessageList.tsx | ⬜ 未着手 | - | - |
| MessageInput.tsx | ⬜ 未着手 | - | - |
| DiaryView.tsx | ⬜ 未着手 | - | - |
| CalendarView.tsx | ⬜ 未着手 | - | - |
| types/index.ts | ⬜ 未着手 | - | - |

### Week 2 目標

| コンポーネント | 状態 | 担当 | 完了日 |
|--------------|------|------|--------|
| Header.tsx | ⬜ 未着手 | - | - |
| AgentSettings.tsx | ⬜ 未着手 | - | - |
| DiaryGenerator.tsx | ⬜ 未着手 | - | - |

進捗記号:
- ⬜ 未着手
- 🔄 作業中
- ✅ 完了
- ⚠️ 問題あり

---

## 🎯 品質チェックリスト

各コンポーネント移植後に確認：

### 機能面
- [ ] 表示が正しい
- [ ] ユーザー操作が動作する
- [ ] エラーハンドリングが適切
- [ ] ローディング状態が表示される
- [ ] データの保存/取得が正常

### コード品質
- [ ] TypeScript型エラーなし
- [ ] ESLintエラーなし
- [ ] Console.log等のデバッグコード削除
- [ ] コメントが適切
- [ ] 命名規則に従っている

### UI/UX
- [ ] モバイルで正常表示
- [ ] タブレットで正常表示
- [ ] デスクトップで正常表示
- [ ] アクセシビリティ対応（aria属性等）
- [ ] キーボード操作可能

### パフォーマンス
- [ ] 不要な再レンダリングなし
- [ ] メモ化が適切（useMemo, useCallback）
- [ ] 大きな画像の最適化
- [ ] 遅延ロード適用（必要に応じて）

---

## 🐛 既知の問題と対処法

### 問題1: IndexedDBからSupabaseへの移行

**問題**: 旧プロジェクトはIndexedDB、新はSupabase

**対処法**:
```typescript
// 旧（IndexedDB）
const data = await db.get('conversations', id);

// 新（Supabase）
const { data, error } = await supabase
  .from('conversations')
  .select('*')
  .eq('id', id)
  .single();
```

### 問題2: OpenAI APIの直接呼び出し

**問題**: 旧はクライアントサイドから直接呼び出し

**対処法**:
```typescript
// 旧（クライアント）
const openai = new OpenAI({ apiKey: userApiKey });

// 新（サーバーサイド）
// app/api/chat/route.ts で処理
// クライアントは fetch('/api/chat') のみ
```

### 問題3: 状態管理の違い

**問題**: 旧はuseStateのみ、新はZustand+TanStack Query

**対処法**:
```typescript
// 旧（useState）
const [messages, setMessages] = useState<MessageData[]>([]);

// 新（Zustand）
const { messages, addMessage } = useChatStore();

// 新（TanStack Query - データフェッチ）
const { data: messages, isLoading } = useQuery({
  queryKey: ['messages'],
  queryFn: fetchMessages
});
```

---

## 📝 移植完了後の最終チェック

全てのコンポーネント移植後：

- [ ] 全ページが正常に表示される
- [ ] エンドツーエンドのユーザーフローが動作する
  - [ ] ログイン → チャット → 日記生成 → 閲覧
- [ ] エラーが発生しない
- [ ] パフォーマンスが許容範囲内
- [ ] セキュリティホールがない
- [ ] ドキュメントが最新

---

## 🎓 学んだこと・改善点（随時更新）

移植作業中に発見した改善点や学びをここに記録：

### 2025-10-18
- 旧プロジェクトのUIコンポーネントは高品質で再利用可能
- 状態管理とデータフェッチの分離が重要
- サーバーサイドでのAPI管理がセキュリティ向上に貢献

（今後追記）

---

**このリストを使って、計画的に移植作業を進めましょう！**
